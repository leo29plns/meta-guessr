{
  "name": "data-guessr",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "lint": "pnpm exec biome lint",
    "lint:fix": "pnpm exec biome lint --write",
    "format": "pnpm exec biome format",
    "format:fix": "pnpm exec biome format --write",
    "type-check": "tsc",
    "check": "pnpm exec biome check && pnpm run type-check",
    "check:fix": "pnpm exec biome check --write",
    "test": "vitest run",
    "dev": "pnpm exec browser-sync start --server 'src/public' --files 'src/public' --no-ui --no-notify",
    "prepare": "husky"
  },
  "lint-staged": {
    "*": [
      "biome check --no-errors-on-unmatched --files-ignore-unknown=true"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.23.0",
  "engines": {
    "node": "24.12.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "@commitlint/cli": "^20.2.0",
    "@commitlint/config-conventional": "^20.2.0",
    "@types/leaflet": "^1.9.21",
    "browser-sync": "^3.0.4",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  }
}
